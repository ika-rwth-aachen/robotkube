apiVersion: apps/v1
kind: Deployment
metadata:
  name: station-00-publisher
  labels:
    tag: robotkube
    app: station-00-publisher
spec:
  replicas: 1
  selector:
    matchLabels:
      app: station-00-publisher
  template:
    metadata:
      labels:
        app: station-00-publisher
    spec:
      terminationGracePeriodSeconds: 0
      nodeSelector:
        node_id: station00
      containers:
      - name: rosbag-play
        image: k3d-robotkube-registry.localhost:41670/robotkube:ros2_bag_files_collective_perception
        imagePullPolicy: IfNotPresent
        command:
          - /bin/bash
          - -c
          - |
            source install/setup.bash
            sleep infinity
